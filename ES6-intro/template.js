const first='hamim'
const last='nusrat'
const name= first + " " + last;
console.log(name);

const a=20;
const b=30
const math=`the sum of ${a} and ${b} is ${a+b}`
console.log(math);

const email='hi jhon'
